# TaskFlow — Frontend

## Setup
1. `cd frontend`
2. `npm install`
3. Create a `.env` file with:

VITE_API_URL=http://localhost:3000

(Backend must be running for this to work locally.)
4. `npm run dev`

App runs on `http://localhost:5173`.